const getWordList = args => {
  let inputValue = args.inputValue;
  let previousValue = args.previousValue;
  let verseText = args.verseText;
  let settings = args.settings;
  
};

export const getCurrentWord = args => {
  let inputValue = args.inputValue;
  let previousValue = args.previousValue;
  let verseText = args.verseText;
  let mode = args.mode;
};